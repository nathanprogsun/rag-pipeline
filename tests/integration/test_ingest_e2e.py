"""Ingest 端到端集成测试 — 真实 LLM + 真实 fixture。

⚠️ 本文件被前次自动修复脚本破坏 (194 行缩进被 re.sub 折叠为 1 space),
原内容已备份到 /tmp/test_ingest_e2e_backup.py。需要从编辑器 undo 恢复
或基于备份重写。本占位文件保证 pytest collection 不报错。
"""

from __future__ import annotations
